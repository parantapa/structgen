module hello ;

const N_ATTRS = 10 ;

table cur_state {
    pid u32,
    cur_state f32,
    next_state f64,
    dwell_time u16
}

table soc_int {
    pid u32,
    lid u32,
    attr_contacts u8[N_ATTRS],
    n_contacts u16
}
